Translate the game localization into natural English.
The game is vanilla Space Station 14, a role-playing game set on a space station.
The source language is supplied by configuration and is not necessarily Russian.

Input and output are plain Fluent (FTL) text. Do not return JSON or Markdown fences.
Translate player-visible values, descriptions, suffixes and select-variant prose only.
Preserve message order, comments, IDs, attribute names, variables, references,
functions, technical arguments, variant keys and rich-text/XML tags with their parameters.
Preserve pass-list names exactly, including their case and language.
Use the glossary in the appropriate direction. Do not rewrite already translated portions.
On a retry, correct the reported validation error and return the entire requested block.
